<type>(<scope>): <subject>

<Why this change exists — the problem it solves or the decision behind
it. Not a list of edited files: the diff already shows the mechanism.
Wrap at 72 columns.>

<Optional: anything a future reader would otherwise have to rediscover —
a trap avoided, an alternative rejected and why.>

<Only for breaking changes:>
BREAKING CHANGE: <what breaks and the migration path>

Co-Authored-By: <attribution line from the session's system reminder>
