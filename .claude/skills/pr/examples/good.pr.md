# Приклад хорошого PR

Реальний PR з цього репо ([#11](https://github.com/Kowloonoriginal/expense-tracker/pull/11)),
скорочений. Орієнтир якості: кожен розділ відповідає на питання
рев'юера, а «Ризики» чесно кажуть, що не перевірено.

---

**Title:** `feat(ui): redesign app in dark navy theme with lime accent`

## Що реалізовано

Редизайн фронтенду за обраним референсом: темно-синій фон, графітові
картки, один лаймовий акцент, «пігулкова» навігація, шрифт Manrope
замість Geist. Підсумок місяця став головним блоком дашборда.

Клас `.paper` перевизначає токени теми, тож будь-який блок можна
перемкнути на світлу палітру без правок у компонентах.

## Ендпоінти

Нових ендпоінтів немає.

## Схема / міграції

Немає.

## Файли

| Файл | Що змінено |
| --- | --- |
| `src/app/globals.css` | Нова палітра токенів, `.paper`, `--income` |
| `src/app/layout.tsx` | Manrope замість Geist |
| `src/widgets/month-summary/ui/MonthSummary.tsx` | Блок-герой, лаймові смужки |

## Як перевірити

```bash
npm run dev:backend
npm run dev:frontend
```

Відкрийте `/`, `/login`, `/dashboard`, `/transactions`, `/categories`.

## Ризики / поза скоупом

- Тема тепер лише темна, перемикача немає.
- Дашборд із реальними даними в браузері не перевірявся.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
