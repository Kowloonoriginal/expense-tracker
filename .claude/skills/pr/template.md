## Що реалізовано

<Результат, який побачить користувач, і навіщо це потрібно. Механізм уже
є в diff — не переказуй файли.>

## Ендпоінти

<Кожен доданий або змінений маршрут: `METHOD /path`, його DTO з
`@repo/shared` і чи він `@Public()` чи за `JwtAuthGuard`.
Якщо нових немає — так і напиши: «Нових ендпоінтів немає».>

## Схема / міграції

<Нові Prisma-моделі чи міграції і чи міграція деструктивна. Або «Немає».>

## Файли

| Файл | Що змінено |
| --- | --- |
| `<path>` | <одним рядком> |

## Як перевірити

```bash
<точні команди>
```

<Маршрут, який відкрити, і що там має бути видно.>

## Ризики / поза скоупом

- <Що свідомо не зроблено, на що дивитися рев'юеру, що не перевірено.>

<Лише для breaking changes:>
BREAKING CHANGE: <що ламається і як мігрувати>

🤖 Generated with [Claude Code](https://claude.com/claude-code)
